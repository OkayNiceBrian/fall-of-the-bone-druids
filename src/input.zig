pub const InputHandler = struct {
    leftPressed: bool,
    rightPressed: bool,
    upPressed: bool,
    downPressed: bool,
    aPressed: bool,
    bPressed: bool,
    xPressed: bool,
    yPressed: bool,
    lPressed: bool,
    rPressed: bool,
    zlPressed: bool,
    zrPressed: bool,
    startPressed: bool,
    selectPressed: bool,

    
};